# SolanaIdeasLab — agent notes

- **Docs vs code:** Root markdown is strategy and market research. Runnable code is under `projects/`.
- **Default implementation target:** `projects/token-safety-bot`. Use `projects/token-sniper-bot` as a reference only unless the task says otherwise.
- **Secrets:** Never commit `.env` or real tokens. Follow each app’s `.env.example`.
- **Edits:** Do not bulk-edit `MASTER-PLAN.md`, `GAP-ANALYSIS.md`, or similar unless the user explicitly requests documentation changes.
